"use client";

import { type ReactNode } from "react";

// Placeholder — smooth scrolling handled by CSS scroll-snap in scene-layout
export function SmoothScrollProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
